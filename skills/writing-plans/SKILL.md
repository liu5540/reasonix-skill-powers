---
name: writing-plans
description: 当你有规格或需求用于多步骤任务时使用，在碰代码之前——将设计分解为小口任务，包含精确文件路径和完整代码
---

# 编写实现计划

## 概述

编写全面的实现计划，假设工程师对我们的代码库零上下文且品味堪忧。记录他们需要知道的一切：每个任务涉及哪些文件、代码、测试、可能需要查阅的文档、如何测试。将整个计划分成小口任务。DRY。YAGNI。TDD。频繁提交。

假设他们是一个有能力的开发者，但对我们的工具集和问题领域几乎一无所知。假设他们不太了解良好的测试设计。

**开始时宣布：** "正在使用编写计划技能来创建实现计划。"

**计划保存到：** `docs/superpowers/plans/<当前日期>-<功能名>-plan.md` （当前日期 通过 `data +%Y-%m-%d` 获取）

## 范围检查

如果规格覆盖多个独立子系统，它应该在头脑风暴阶段被拆成子项目规格。如果没有，建议将其拆分为独立计划——每个子系统一个。每个计划应该能独立产出可工作的、可测试的软件。

## 文件结构

在定义任务前，先规划哪些文件会被创建或修改，每个文件负责什么。这是分解决策锁定的地方。

- 设计具有清晰边界和良好定义接口的单元。每个文件应该有一个清晰的职责。
- 你最能推理能一次放入上下文的代码，当文件聚焦时编辑更可靠。偏好更小、聚焦的文件而非做太多的大文件。
- 一起变更的文件应该放在一起。按职责拆分，而非按技术层次。
- 在已有代码库中，遵循已建立的模式。如果代码库使用大文件，不要单方面重构——但如果你正在修改的文件已经变得笨重，在计划中包含拆分是合理的。

这个结构为任务分解提供信息。每个任务应该产出独立有意义的变更。

## 小口任务粒度

**每个步骤一个动作（2-5 分钟）：**
- "编写失败的测试" — 一个步骤
- "运行确认它失败" — 一个步骤
- "实现最小代码使测试通过" — 一个步骤
- "运行测试确认通过" — 一个步骤
- "提交" — 一个步骤

## 计划文档头部

**每个计划必须以这个头部开始：**

```markdown
# [功能名称] 实现计划

> **面向代理工作者：** 必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 来逐任务实现此计划。步骤使用 checkbox（`- [ ]`）语法进行跟踪。

**目标：** [一句话描述要构建什么]

**架构：** [2-3 句话关于方法]

**技术栈：** [关键技术/库]

---
```

## 任务结构

````markdown
### 任务 N：[组件名称]

**文件：**
- 创建：`确切的/路径/到/文件.go`
- 修改：`确切的/路径/到/已有.go:123-145`
- 测试：`确切的/路径/到/测试.go`

- [ ] **步骤 1：编写失败的测试**

```go
func Test具体行为(t *testing.T) {
    result := Function(input)
    if result != expected {
        t.Errorf("期望 %v，得到 %v", expected, result)
    }
}
```

- [ ] **步骤 2：运行测试验证它失败**

运行：`go test ./pkg/path/ -run TestName -v`
期望：FAIL，因为"function not defined"

- [ ] **步骤 3：编写最小实现**

```go
func Function(input string) string {
    return expected
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`go test ./pkg/path/ -run TestName -v`
期望：PASS

- [ ] **步骤 5：提交**

```bash
git add pkg/path/test.go pkg/path/file.go
git commit -m "feat: 添加具体功能"
```
````

## 禁止占位符

每个步骤必须包含工程师需要的实际内容。以下是**计划失败**——绝不要写：
- "TBD"、"TODO"、"稍后实现"、"填写细节"
- "添加适当的错误处理" / "添加验证" / "处理边界情况"
- "为以上编写测试"（没有实际测试代码）
- "类似任务 N"（重复代码——工程师可能乱序阅读任务）
- 描述要做什么却不展示怎么做的步骤（代码步骤需要代码块）
- 引用在任何任务中未定义的类型、函数或方法

## 记住
- 始终使用精确文件路径
- 每个步骤的完整代码——如果步骤涉及代码变更，展示代码
- 精确命令带期望输出
- DRY、YAGNI、TDD、频繁提交

## 自审

写完完整计划后，用新眼光对照规格检查计划。这是你自己运行的检查清单——不是子代理分派。

**1. 规格覆盖：** 浏览规格中的每个部分/需求。能指出哪个任务实现了它吗？列出任何缺口。

**2. 占位符扫描：** 搜索你的计划中是否有上述"禁止占位符"部分的任何模式。修复它们。

**3. 类型一致性：** 后续任务中使用的类型、方法签名和属性名与早期任务中定义的一致吗？任务3中叫`clearLayers()`的函数在任务7中叫`clearFullLayers()`就是一个 bug。

如果发现问题，内联修复。不需要重新审查——修完即走。如果发现规格需求没有对应任务，添加任务。

## 执行交接

保存计划后，提供执行选项：

**"计划完成并保存到 `docs/superpowers/plans/<文件名>.md`。两个执行选项：**

**1. 子代理驱动（推荐）** — 每任务分派全新子代理，任务间审查，快速迭代

**2. 内联执行** — 使用 executing-plans 在当前会话中执行，批量执行带检查点

**选择哪种方式？"**

**如果选择子代理驱动：**
- **必需子技能：** 使用 subagent-driven-development
- 每任务全新子代理 + 两阶段审查

**如果选择内联执行：**
- **必需子技能：** 使用 executing-plans
- 批量执行带检查点供审查
